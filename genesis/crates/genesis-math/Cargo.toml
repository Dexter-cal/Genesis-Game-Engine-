[package]
name = "genesis-math"
version.workspace = true
edition.workspace = true
description = "Math utilities for Genesis - vectors, matrices, quaternions, noise"
[dependencies]
serde = { workspace = true }
bytemuck = { workspace = true }
rand = { workspace = true }
